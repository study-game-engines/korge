artifact "korge:engine"

source {
    common {
        include project(":korge:core")
        include "org.jetbrains.kotlinx:kotlinx-coroutines-debug:$kotlinx_coroutines_version"
    }
    desktop {
        include project(":korge:ipc-desktop")
    }
}
